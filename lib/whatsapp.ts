// Kept in sync with the backend's MSG_PREFIX (notifications.service.ts).
// Manual sends are composed/previewed client-side, so the prefix is added
// here rather than server-side, so what the user sees in the compose dialog
// is exactly what gets sent.
export const MSG_PREFIX = 'A2insurance care: ';
